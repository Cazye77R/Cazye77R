// Global simulation constants — single source of truth

// Tile dimensions (px)
export const TILE_W = 64;
export const TILE_H = 32;

// Isometric projection vertical scale per Z unit
export const Z_SCALE = 16;

// Canvas origin offset so the grid is centered on screen
export const ORIGIN_X = 400;
export const ORIGIN_Y = 80;

// Grid dimensions
export const GRID_COLS = 11;
export const GRID_ROWS = 9;

// Agent movement speed (tiles per second)
export const MOVE_SPEED = 2.6;

// Base simulation tick (ms)
export const TICK_MS = 1000;
