"""
LocalTranscribe – Streamlit web application.

Entry point for the UI. Lets users upload an audio file, configure
transcription settings, run the pipeline, and explore results
(transcript, speaker labels, summary, action items, Q&A).
"""

# TODO: import streamlit as st
# TODO: import os, pathlib
# TODO: from transcriber import Transcriber
# TODO: from diarizer import Diarizer
# TODO: from llm_processor import LLMProcessor
# TODO: from config import SUPPORTED_FORMATS, OUTPUT_DIR, WHISPER_MODEL, OLLAMA_BASE_URL


def main():
    """Configure the Streamlit page and render the full application UI."""

    # TODO: st.set_page_config(title="LocalTranscribe", layout="wide")
    # TODO: render sidebar with model/device/Ollama settings
    # TODO: render file uploader restricted to SUPPORTED_FORMATS
    # TODO: render "Transcribe" button that triggers the pipeline
    # TODO: display progress bar and status messages during processing
    # TODO: display transcript table with timestamps and speaker labels
    # TODO: render LLM section (summary, action items, Q&A input)
    # TODO: provide download buttons for .txt and .json exports
    pass


if __name__ == "__main__":
    main()
